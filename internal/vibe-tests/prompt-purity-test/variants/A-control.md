<!--
Control condition: NO self-check block is appended.
This file is intentionally comment-only. setup-purity.mjs strips HTML comments and,
finding no remaining text, injects nothing — so the agent sees the astryx-init prompt
exactly as `generateCompressedIndex()` emits it today. This is the baseline the
candidate variants (B, C) are measured against.
-->
