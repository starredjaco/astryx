# /packages

Published npm packages for the XDS design system.

<!-- SYNC: When files in this directory change, update this document. -->

| Directory | Role | Purpose |
|-----------|------|---------|
| `core/` | Primary | Core UI components library (`@xds/core`) |

